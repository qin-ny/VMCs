package controller;

/**
 * the interface offers the entrance of GUI generation of maintainer panel as well as
 * the data manipulation to the machine.
 */
public interface InterfaceMaintainerController {
    /**
     * generate the GUI interface of maintainer panel
     */
    void generateView();
}
